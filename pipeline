pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git 'https://github.com/harismuneer/Flight-Booking-System-JavaServlets_App.git'
            }
        }
        // Add more stages for building, testing, deploying, etc.
    }
}
